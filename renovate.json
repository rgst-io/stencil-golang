{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update tool versions in .mise.toml.tpl",
      "fileMatch": ["^templates/.mise.toml.tpl$"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+?)(?: depName=(?<depName>.+?))? packageName=(?<packageName>.+?)(?: versioning=(?<versioning>[a-z-]+?))?\\s- [a-z-]+: \"(?<currentValue>.+?)\"\\s"
      ]
    },
    {
      "customType": "regex",
      "description": "Update workflow versions in .github/workflows",
      "fileMatch": ["^.github/workflows/[a-z-]+.yml$"],
      "matchStrings": [
        "{{- /\\* renovate: datasource=(?<datasource>[a-z-]+?)(?: depName=(?<depName>.+?))? packageName=(?<packageName>.+?)(?: versioning=(?<versioning>[a-z-]+?))? \\*/}}\\s+(?:- )?uses: [a-z-/]+@(?<currentValue>.+?)\\s"
      ]
    }
  ]
}
